export type User = {
	email: string;
	password: string;
};

export type NewUser = {
	firstName: string;
	lastName: string;
	email: string;
	password: string;
};

export type HomePageUser = {
	firstName: string;
	lastName: string;
};
